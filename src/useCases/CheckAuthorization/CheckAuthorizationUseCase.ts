// Deve checar o mock para ver se podemos realizar a transação
import axios from "axios";
import { Mock } from "../../utils/mock";

export class CheckAuthorizationUseCase {
  async execute() {
    return true;
  }
}
